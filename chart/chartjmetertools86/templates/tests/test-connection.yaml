apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chartjmetertools86.fullname" . }}-test-connection"
  labels:
    {{- include "chartjmetertools86.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chartjmetertools86.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
